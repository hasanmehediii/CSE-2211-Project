# PostgreSQL connection string used by the FastAPI application.
# Copy this file to .env and replace the values for your database.
DATABASE_URL=postgresql://postgres:password@localhost:5432/goriber_gari?sslmode=prefer&channel_binding=prefer

# Optional: URL allowed to call the API when the frontend is hosted elsewhere.
FRONTEND_URL=http://localhost:5173
